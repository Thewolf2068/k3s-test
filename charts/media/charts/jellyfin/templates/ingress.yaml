apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: jellyfin
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`jelly.miquella.dev`)
      kind: Rule
      services:
        - name: jellyfin
          port: {{ .Values.port }}
  tls:
    certResolver: cloudflare
