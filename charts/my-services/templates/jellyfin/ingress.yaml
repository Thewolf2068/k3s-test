apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: jellyfin
  namespace: jellyfin
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`jelly.miquella.dev`)
      kind: Rule
      services:
        - name: jellyfin
          port: {{ .Values.services.jellyfin.port }}
